var documenterSearchIndex = {"docs":
[{"category":"section","location":"bezier_extraction/#Bezier-extraction","page":"Bezier extraction","text":"The basefunctions used in IGA (B-splines, NURBS, etc.) exist over multiple adjecent elements. This is in contrast to traditional finite element method, where identical shape function are defined withing all elements. This makes it difficult to incorprate IGA in to a standard finite element code. The bezier extraction technique, introduced by Borden et.al, solves this problem by allowing numerical integration of smooth function to be performed on C0 Bezier elements.\n\nThe C^p-continiuos B-Spline basis functions on an element, boldsymbol N^e can be computeded from the C^0 bernstein polynominals, boldsymbol B^e, as\n\n    boldsymbol N^e = boldsymbol C^e boldsymbol B^e\n\nwhere boldsymbol C^e is the bezier extraction operator for the current cell. This operator can be pre-computed for each element in the IGA-mesh. With the help of this relation, it can be shown that the NURBS can be computed as \n\n    boldsymbol R^e = boldsymbol W^e frac boldsymbol N^eW(xi) = boldsymbol W^e boldsymbol C^e fracboldsymbol B^eW^b(xi)\n\nwhere boldsymbol W^e is a diagonal matrix of the rational weights, w_I I = 12dotsN, and W(xi) and W^b(xi) are the weight functions \n\n    W(xi) = W^b(xi) = sum_I^N = N(xi)  w_I = sum_I^N = B(xi)  w^b_I  \n\nIt can also be show that a NURBS-surface (or curve/solid) can be represented using the Bernstein basis functions,\n\n    S(xi eta) = boldsymbol X^e boldsymbol R(xi eta) = boldsymbol X_b^e boldsymbol B(xi eta)\n\nwhere boldsymbol X^e are the control points for the NURBS surface, and boldsymbol X_b^e are controlpoints on the bezier element, caluculated as \n\n    boldsymbol X_b^e = frac1W(xi) left(boldsymbol C^eright)^T boldsymbol W^e boldsymbol X^e\n\nThe equations above show that we can pre-compute the bernstein basis values at some gauss points (similar to how we pre compute the shape values for Lagrange basis functions), and then using the bezier extraction operator, calculate the NURBS values. In the code, the reinitilzation of the shape functions is performed like this.\n\nip = Bernstein{2,orders}()\nqr_cell = QuadratureRule{2,RefCube}(4)\n\ncv = BezierCellValues( CellVectorValues(qr_cell, ip) )\n\n#...\n\nextr = get_extraction_operator(grid, cellid) # Extraction operator\nX = getcoordinates(grid, cellid) #Nurbs coords\nw = getweights(grid, cellid)       #Nurbs weights\nwᴮ = compute_bezier_points(extr, w)\nXᴮ = inv.(wᴮ) .* compute_bezier_points(extr, w.*X)\n\nreinit!(cv, (Xᴮ, wᴮ))","title":"Bezier extraction"},{"category":"section","location":"bezier_values/#BezierValues.jl","page":"BezierValues.jl","text":"Documentation for IGA.jl","title":"BezierValues.jl"},{"category":"section","location":"examples/plate_with_hole/#Infinite-plate-with-hole","page":"Infinite plate with hole","text":"(Image: )\n\nIn this example we will solve a simple elasticity problem; an infinite plate with a hole. The main goal of the tutorial is to show how one can solve the problem using Isogeometric Analysis (IGA), or in other words, solving a FE-problem with splines as the basis/shape functions. By using so called bezier extraction, we will see that most of the structure of the code will be the same as in standard FE-codes (however many differences are happening \"under the hood\").\n\nnote: Note\nIt is expected that the reader already be familiar with IGA and the concept of \"bezier extraction\". It is also expected that the reader is familiar with the Ferrite package. In particular Ferrite.DofHandler and Ferrite.CellValues.\n\nStart by loading the necessary packages\n\nusing Ferrite, IGA, LinearAlgebra\n\nNext we define the functions for the integration of the element stiffness matrix and traction force. These functions will be the same as for a normal finite elment problem, but with the difference that we need the cell coorinates AND cell weights (the weights from the NURBS shape functions), to reinitilize the shape values, dNdx. Read this page, to see how the shape values are reinitilized.\n\nfunction integrate_element!(ke::AbstractMatrix, C::SymmetricTensor{4,2}, cv)\n    n_basefuncs = getnbasefunctions(cv)\n\n    δɛ = [zero(SymmetricTensor{2,2,Float64}) for i in 1:n_basefuncs]\n    for q_point in 1:getnquadpoints(cv)\n\n        for i in 1:n_basefuncs\n            δɛ[i] = symmetric(shape_gradient(cv, q_point, i))\n        end\n\n        dΩ = getdetJdV(cv, q_point)\n        for i in 1:n_basefuncs\n            for j in 1:n_basefuncs\n                ke[i, j] += (δɛ[i] ⊡ C ⊡ δɛ[j]) * dΩ\n            end\n        end\n    end\nend;\n\nfunction integrate_traction_force!(fe::AbstractVector, t::Vec{2}, fv)\n    n_basefuncs = getnbasefunctions(fv)\n\n    for q_point in 1:getnquadpoints(fv)\n        dA = getdetJdV(fv, q_point)\n        for i in 1:n_basefuncs\n            δu = shape_value(fv, q_point, i)\n            fe[i] += t ⋅ δu * dA\n        end\n    end\nend;\nnothing #hide\n\nThe assembly loop is also written in almost the same way as in a standard finite element code. The key differences will be described in the next paragraph,\n\nfunction assemble_problem(dh::DofHandler, grid, cv, fv, stiffmat, traction)\n\n    f = zeros(ndofs(dh))\n    K = allocate_matrix(dh)\n    assembler = start_assemble(K, f)\n\n    n = getnbasefunctions(cv)\n    celldofs = zeros(Int, n)\n    fe = zeros(n)     # element force vector\n    ke = zeros(n, n)  # element stiffness matrix\n\n    n = Ferrite.nnodes_per_cell(grid)\n\tw = zeros(Float64, n)\n\tx = zeros(Vec{2}, n)\n\twb = zeros(Float64, n)\n\txb = zeros(Vec{2}, n)\n\n    # Assemble internal forces\n    for cellid in 1:getncells(grid)\n        fill!(fe, 0.0)\n        fill!(ke, 0.0)\n        celldofs!(celldofs, dh, cellid)\n\n        # In a normal finite elment code, this is the point where we usually get the coordinates of the element `X = getcoordinates(grid, cellid)`. In this case, however,\n        # we also require the cell weights, and we need to transform them to the bezier mesh.\n        extr = get_extraction_operator(grid, cellid) # Extraction operator\n        get_bezier_coordinates!(xb,wb,x,w,grid,cellid) #Nurbs coords\n        set_bezier_operator!(cv, extr, w)\n        reinit!(cv, (xb,wb)) ## Reinit cellvalues by passsing both bezier coords and weights\n        integrate_element!(ke, stiffmat, cv)\n\n        assemble!(assembler, celldofs, ke, fe)\n    end\n\n    # Assamble external forces\n    for (cellid, faceid) in getfacetset(grid, \"left\")\n        fill!(fe, 0.0)\n\n        celldofs!(celldofs, dh, cellid)\n\n        beziercoords = getcoordinates(grid, cellid)\n        reinit!(fv, beziercoords, faceid)\n\n        integrate_traction_force!(fe, traction, fv)\n        f[celldofs] += fe\n    end\n\n\n    return K, f\nend;\nnothing #hide\n\nThis is a function that returns the elastic stiffness matrix\n\nfunction get_material(; E, ν)\n    λ = E*ν / ((1 + ν) * (1 - 2ν))\n    μ = E / (2(1 + ν))\n    δ(i,j) = i == j ? 1.0 : 0.0\n    g(i,j,k,l) = λ*δ(i,j)*δ(k,l) + μ*(δ(i,k)*δ(j,l) + δ(i,l)*δ(j,k))\n\n    return SymmetricTensor{4, 2}(g)\nend;\nnothing #hide\n\nWe also create a function that calculates the stress in each quadrature point, given the cell displacement and such...\n\nfunction calculate_stress(dh, cv::BezierCellValues, C::SymmetricTensor{4,2}, u::Vector{Float64})\n\n    celldofs = zeros(Int, ndofs_per_cell(dh))\n\n    #Store the stresses in each qp for all cells\n    cellstresses = Vector{SymmetricTensor{2,2,Float64,3}}[]\n\n    for cellid in 1:getncells(dh.grid)\n\n        bc = getcoordinates(dh.grid, cellid)\n\n        reinit!(cv, bc)\n        celldofs!(celldofs, dh, cellid)\n\n        ue = u[celldofs]\n        qp_stresses = SymmetricTensor{2,2,Float64,3}[]\n        for qp in 1:getnquadpoints(cv)\n            ε = symmetric(function_gradient(cv, qp, ue))\n            σ = C ⊡ ε\n            push!(qp_stresses, σ)\n        end\n        push!(cellstresses, qp_stresses)\n    end\n\n    return cellstresses\nend;\nnothing #hide\n\nNow we have all the parts needed to solve the problem. We begin by generating the mesh. IGA.jl includes a couple of different functions that can generate different nurbs patches. In this example, we will generate the patch called \"plate with hole\". Note, currently this function can only generate the patch with second order basefunctions.\n\norder = 2 # order of the NURBS\nnels = (20, 10) # Number of elements\nnurbsmesh = generate_nurbs_patch(:plate_with_hole, nels, order);\nnothing #hide\n\nPerforming the computation on a NURBS-patch is possible, but it is much easier to using \"bezier-extraction\". For this we transform the NURBS-patch into a BezierGrid. The BezierGrid is identical to the standard Ferrite.Grid, but includes the NURBS-weights and bezier extraction operators.\n\ngrid = BezierGrid(nurbsmesh);\nnothing #hide\n\nNext, create some facesets. This is done in the same way as in normal Ferrite-code. One thing to note however, is that the nodes/controlpoints, does not necessary lay exactly on the geometry due to the non-interlapotry nature of NURBS spline functions. However, in most cases they will be close enough to use the Ferrite functions below.\n\naddnodeset!(grid, \"right\", (x) -> x[1] ≈ -0.0)\naddfacetset!(grid, \"left\", (x) -> x[1] ≈ -4.0)\naddfacetset!(grid, \"bot\", (x) -> x[2] ≈ 0.0)\naddfacetset!(grid, \"right\", (x) -> x[1] ≈ 0.0);\nnothing #hide\n\nCreate the cellvalues storing the shape function values. Note that the CellVectorValues/FaceVectorValues are wrapped in a BezierValues. It is in the reinit-function of the BezierValues that the actual bezier transformation of the shape values is performed.\n\nip_geo = IGAInterpolation{RefQuadrilateral,order}()\nip_u = ip_geo^2\nqr_cell = QuadratureRule{RefQuadrilateral}(4)\nqr_face = FacetQuadratureRule{RefQuadrilateral}(3);\n\ncv = BezierCellValues(qr_cell, ip_u)\nfv = BezierFacetValues(qr_face, ip_u);\nnothing #hide\n\nDistribute dofs as normal\n\ndh = DofHandler(grid)\nadd!(dh, :u, ip_u)\nclose!(dh);\n\nae = zeros(ndofs(dh))\nIGA.apply_analytical_iga!(ae, dh, :u, x -> x);\nnothing #hide\n\nAdd two symmetry boundary condintions. Bottom face should only be able to move in x-direction, and the right boundary should only be able to move in y-direction\n\nch = ConstraintHandler(dh)\ndbc1 = Dirichlet(:u, getfacetset(grid, \"bot\"), (x, t) -> 0.0, 2)\ndbc2 = Dirichlet(:u, getfacetset(grid, \"right\"), (x, t) -> 0.0, 1)\nadd!(ch, dbc1)\nadd!(ch, dbc2)\nclose!(ch)\nupdate!(ch, 0.0);\nnothing #hide\n\nDefine stiffness matrix and traction force\n\nstiffmat = get_material(E=100, ν=0.3)\ntraction = Vec((-10.0, 0.0))\nK, f = assemble_problem(dh, grid, cv, fv, stiffmat, traction);\nnothing #hide\n\nSolve\n\napply!(K, f, ch)\nu = K \\ f;\nnothing #hide\n\nNow we want to export the results to VTK. So we calculate the stresses in each gauss-point, and project them to the nodes using the L2Projector from Ferrite. Node that we need to create new CellValues of type CellScalarValues, since the L2Projector only works with scalar fields.\n\ncellstresses = calculate_stress(dh, cv, stiffmat, u);\nnothing #hide\n\nL2 projections currently broken for IGA\n\nprojector = L2Projector(ip_u, grid)\nσ_nodes = project(projector, cellstresses, qr_cell)\n\nIGA.VTKIGAFile(\"plate_with_hole.vtu\", grid) do vtk\n    write_solution(vtk, dh, u)\n    write_projection(vtk, projector, σ_nodes, \"σ\")\nend;\nnothing #hide\n\n\n\nThis page was generated using Literate.jl.","title":"Infinite plate with hole"},{"category":"section","location":"examples/structuralvibrations/#Structural-vibrations","page":"Structural vibrations","text":"In this example, we will compare traditional Finite Element Method (FEM) with Isogeometric Analysis (IGA) for analysing structural vibration problems. We aim to replicate the results from Cottrell, J. et al, Isogeometric analysis of structural vibrations, In Computer Methods in Applied Mechanics and Engineering https://doi.org/10.1016/j.cma.2005.09.027\n\nConsider the structural vibrations of an elastic rod of unit length, whose natural frequencies and modes are goverened by:\n\nu_xx + omega_n^2 u = 0 quad x in (01)\n\nsubjected to boundary conditions:\n\nu(0) = u(1) = 0\n\nwhere u is the displacement field, and omega_n is n:th natural frequency.\n\nAfter discretisation, we formulate the generalised eigenvalue problem, which allows us to solve for the natural frequencies and modes boldsymbolphi_n:\n\n(boldsymbolK - omega^2_n boldsymbolM) boldsymbolphi_n = 0\n\nHere, boldsymbolK and boldsymbolM are the standard stiffness and mass matrices.","title":"Structural vibrations"},{"category":"section","location":"examples/structuralvibrations/#Main-code","page":"Structural vibrations","text":"Per usual, we first load the relevant packages\n\nusing Ferrite, IGA, LinearAlgebra, Plots\n\nNext we define the element routine used to compute the element stiffness and mass matrices\n\nfunction stiffness_and_mass_matrix!(ke, me, cv)\n    for qp in 1:getnquadpoints(cv)\n        dV = getdetJdV(cv,qp)\n        for i in 1:getnbasefunctions(cv)\n            for j in 1:getnbasefunctions(cv)\n                ke[i,j] += shape_gradient(cv, qp, i) ⋅ shape_gradient(cv, qp, j) * dV\n                me[i,j] += shape_value(cv, qp, i) * shape_value(cv, qp, j) * dV\n            end\n        end\n    end\nend;\nnothing #hide\n\nWe also create a function for computing the natural frequencies omega_n. The input for the function is the grid (either a FEM or IGA mesh), and the corresponing cellvalues and interpolations (either Lagrange or IGAInterpolaiton).\n\nfunction compute_eigenvalues(grid, cellvalues, ip)\n\n    #Create dofhandler\n    dh = DofHandler(grid)\n    add!(dh, :u, ip)\n    close!(dh)\n\n    #initlize matrices\n    nbf = getnbasefunctions(ip)\n    ke = zeros(nbf,nbf)\n    me = zeros(nbf,nbf)\n    K = allocate_matrix(dh)\n    M = allocate_matrix(dh)\n\n    #assemble system\n    assembler_K = start_assemble(K)\n    assembler_M = start_assemble(M)\n    for cellid in 1:getncells(grid)\n        fill!(ke, 0.0)\n        fill!(me, 0.0)\n        coords = getcoordinates(grid, cellid)\n        dofs = celldofs(dh, cellid)\n\n        reinit!(cellvalues, coords)\n        stiffness_and_mass_matrix!(ke, me, cellvalues)\n\n        assemble!(assembler_K, dofs, ke)\n        assemble!(assembler_M, dofs, me)\n    end\n\n    #apply BC\n    ch = ConstraintHandler(dh)\n    add!(ch, Dirichlet(:u, getfacetset(grid, \"left\"), x->(0.0,)))\n    add!(ch, Dirichlet(:u, getfacetset(grid, \"right\"), x->(0.0,)))\n    close!(ch)\n    apply!(K, ch)\n\n    #Solve generlized eigenvalue problem\n    λ, ϕ = eigen(Matrix(K), Matrix(M)) #Note, we need to convert to full matrices\n    ω = sqrt.(λ)\n    return ω\nend;\nnothing #hide\n\nWe can now solve for the natural frequencies for both the FEM and IGA case. We will use quadratic shape functions and a total of 999 DOFs.","title":"Main code"},{"category":"section","location":"examples/structuralvibrations/#Finite-element-solution","page":"Structural vibrations","text":"const N = 999    # Number of desired dofs\nconst order = 2  # Order of polynomial\nconst L = 1.0    # Length of beam\nconst nel_fem = (N-1) ÷ order; # Number of elements\ngrid = generate_grid(Line, (nel_fem,), Vec(0.0), Vec(L))\n\nip = Lagrange{RefLine,order}()\nqr = QuadratureRule{RefLine}(6)\ncellvalues = CellValues(qr, ip)\n\nω_fem = compute_eigenvalues(grid, cellvalues, ip);\nnothing #hide","title":"Finite element solution"},{"category":"section","location":"examples/structuralvibrations/#Isogeometric-solution","page":"Structural vibrations","text":"const nel_iga = N-order # Number of elements\ngrid = generate_grid(BezierCell{RefLine,order}, (nel_iga,), Vec(0.0), Vec(L))\n\nip = IGAInterpolation{RefLine,order}()\nqr = QuadratureRule{RefLine}(6)\ncellvalues = BezierCellValues(qr, ip)\n\nω_iga = compute_eigenvalues(grid, cellvalues, ip);\nnothing #hide","title":"Isogeometric solution"},{"category":"section","location":"examples/structuralvibrations/#Results","page":"Structural vibrations","text":"Now, we can plot and compare the normalised solutions. From the results, we observe that the accuracy of the FEM solution diminishes significantly for nN05. This highlights the advantageous properties of IGA for structural dynamics problems.\n\nanalytical_ω_f(n) = π*n\nω_analytical = analytical_ω_f.(1:N)\n\nnormalised_ω_fem  = ω_fem ./ ω_analytical\nnormalised_ω_iga  = ω_iga ./ ω_analytical\n\nn_range = range(0,1,N)\nfig = plot(; title=\"Eigenvalues\", ylabel = \"Normalised eigenvalue\", xlabel=\"Normalised eigen number\")\nplot!(fig, n_range, normalised_ω_fem, label=\"Quadratic FEM\")\nplot!(fig, n_range, normalised_ω_iga, label=\"Quadratic IGA\")\n\n\n\nThis page was generated using Literate.jl.","title":"Results"},{"category":"section","location":"bsplines_nurbs/#Splines","page":"Splines","text":"","title":"Splines"},{"category":"section","location":"bsplines_nurbs/#B-Splines","page":"Splines","text":"The univariate B-Spline basis are defined by a knot vector, which is a non-decreasing parametric coordinate written ad Xi = xi_1  xi_n+p+1, where p is the polynominal degree of the  basis function, and n is the number of basis functions. The B-splines are defined recursivily as \n\n    hat N_Ap(xi) = fracxi - xi_Axi_A+p - xi_A hat N_Ap-1(xi) + fracxi_A+p+1 - xixi_A+p+1 - xi_A+1 hat N_A+1p-1(xi)\n\nand \n\n    hat N_A0(xi) = \n    begincases\n    1  xi_a leq xi  xi_A+1\n    0  textotherwise\n    endcases\n\nThe B-Spline basis functions can be used the define geometries (curve/surface/solid). Below, an example of a B-Splines surface is presented,\n\n    boldsymbol S(xieta) = sum_A=1^N boldsymbol X_A N_A(xi eta)\n\nwhere boldsymbol X_A are the control points (coordinates), and N_A are a combination of two univariate B-spline functions in different parametric directions,\n\n    N_Axi eta = hat N_i^xi(xi) cdot hat N_j^eta(eta)\n\nHere, hat N_i^xi and hat N_j^eta have their own set of knot vectors.","title":"B-Splines"},{"category":"section","location":"bsplines_nurbs/#NURBS","page":"Splines","text":"TODO","title":"NURBS"},{"category":"section","location":"#IGA.jl","page":"Home","text":"Small toolbox for Isogeometric anlysis. Built on top of Ferrite","title":"IGA.jl"},{"category":"section","location":"#Installation","page":"Home","text":"pkg> add https://github.com/lijas/IGA.jl.git","title":"Installation"},{"category":"section","location":"#About-IGA","page":"Home","text":"From wikipedia: \n\nIsogeometric analysis is a computational approach that offers the possibility of integrating finite element analysis (FEA) into conventional NURBS-based CAD design tools. Currently, it is necessary to convert data between CAD and FEA packages to analyse new designs during development, a difficult task since the two computational geometric approaches are different. Isogeometric analysis employs complex NURBS geometry (the basis of most CAD packages) in the FEA application directly. This allows models to be designed, tested and adjusted in one go, using a common data set.","title":"About IGA"}]
}
